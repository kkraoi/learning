import java.util.Date;
import java.util.Calendar;
import java.util.SimpleDateFromat;

public class Answer {
  public static void main(String[] args) {
    // StringBuilder sb = new StringBuilder();
    // for (int i = 1; i <= 100; i++) {
    //   sb.append(i).append(",");
    // } 
    // String s = sb.toString();

    // String[] a = s.split(",");

    // String folder = "c:¥javadev";
    // String folder1 = "c:¥user¥";
    // String file = "readme.txt";
    // Strin filePath = buildFilePath(folder, file);
    // Strin filePath1 = buildFilePath(folder1, file);
    
    // // folder と file を連結して完全あnファイル名を組み立てるメソッド
    // public String buildFilePath(String folder, String file) {
    //   if (!folder.endsWith("¥¥")) {
    //     folder += "¥¥"
    //   }
    //   return folder + file
    // }

    // 全てのすべての文字列
    // "ほげ".matches(".*");
    // "A23".matches("A[0-9]?");
    // "A2".matches("A[0-9]?");
    // "UHOG".matches("U[A-Z]{3}")
    
    // Date d = new Date();
    // Calendar c = Calendar.getInstance();
    // c.setTime(d);
    // int day = c.get(Calendar.DAY_OF_MONTH);
    // c.set(Calendar.DAY_OF_MONTH, day + 100);
    // Date d1 = c.getTime();
    // SimpleDateFormat f = new SimpleDateFormat("西暦yyyy年MM月dd日");
    // Date d2 = f.format(d1);

  
  }
}